# Enterprise policy

Enterprise policy controls file transfer, clipboard, remote input, MFA, max session length and device eligibility.
Remote input should only become available when MFA is required and host/session policy allows it.
