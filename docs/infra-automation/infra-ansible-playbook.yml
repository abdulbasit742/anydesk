---
- hosts: remotedesk
  become: yes
  tasks:
    - name: Install Docker
      apt:
        name: docker.io
        state: present
    
    - name: Start services
      docker_compose:
        project_src: /opt/remotedesk
        state: present
