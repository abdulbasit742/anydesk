# SDK client guide

The generated RemoteDeskApiClient is a small typed fetch wrapper. It does not add dependencies.
Wire getAccessToken to the current auth provider and keep token refresh outside the SDK.
