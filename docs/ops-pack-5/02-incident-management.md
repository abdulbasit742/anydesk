# Incident management model

Incidents include severity, status and updates.
SEV1 covers data loss suspicion, login blocking or large active-session impact.
Public status updates should avoid user data, IPs, tokens, session IDs and internal stack traces.
