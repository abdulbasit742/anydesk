# Device enrollment

Enrollment codes should be short-lived, one-time claimable and audited.
Claiming a code should bind device fingerprint, user/team and app version.
Do not expose raw code hashes in dashboard UI.
