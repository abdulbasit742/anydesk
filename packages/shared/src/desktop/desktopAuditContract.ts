export type DesktopAuditAction = "desktop.session.started" | "desktop.session.ended" | "desktop.file.offered" | "desktop.file.accepted" | "desktop.file.rejected" | "desktop.file.completed" | "desktop.file.failed" | "desktop.clipboard.enabled" | "desktop.clipboard.disabled" | "desktop.clipboard.synced" | "desktop.input.blocked" | "desktop.emergency_stop";
export interface DesktopAuditPayload { action: DesktopAuditAction; sessionId: string; occurredAt: string; actorDeviceId?: string; targetDeviceId?: string; metadata?: Record<string, string | number | boolean | null>; }
export function buildDesktopAuditPayload(input: Omit<DesktopAuditPayload, "occurredAt"> & { occurredAt?: string }): DesktopAuditPayload { return { ...input, occurredAt: input.occurredAt ?? new Date().toISOString() }; }
