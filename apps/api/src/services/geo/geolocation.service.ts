export const geoService = { async getLocationByIP(ip: string): Promise<{ country: string; city: string; lat: number; lon: number; timezone: string }> { return { country: "US", city: "San Francisco", lat: 37.7749, lon: -122.4194, timezone: "America/Los_Angeles" }; }, async getDistance(lat1: number, lon1: number, lat2: number, lon2: number): Promise<number> { const R = 6371; const dLat = (lat2 - lat1) * Math.PI / 180; const dLon = (lon2 - lon1) * Math.PI / 180; const a = Math.sin(dLat / 2) ** 2 + Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) * Math.sin(dLon / 2) ** 2; return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a)); }, async getNearestRelay(lat: number, lon: number): Promise<{ id: string; location: string; latency: number }> { return { id: "relay_us_west", location: "US West", latency: 12 }; } };
