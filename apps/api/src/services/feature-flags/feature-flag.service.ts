export const featureFlagService = { flags: new Map<string, { enabled: boolean; rollout: number; allowList: string[]; denyList: string[] }>(), async isEnabled(flagName: string, userId?: string): Promise<boolean> { const flag = this.flags.get(flagName); if (!flag) return false; if (!flag.enabled) return false; if (userId && flag.denyList.includes(userId)) return false; if (userId && flag.allowList.includes(userId)) return true; if (flag.rollout >= 100) return true; if (userId) { const hash = userId.split("").reduce((a, c) => a + c.charCodeAt(0), 0) % 100; return hash < flag.rollout; } return flag.rollout >= 50; }, async setFlag(name: string, config: { enabled: boolean; rollout?: number }) { this.flags.set(name, { enabled: config.enabled, rollout: config.rollout || 100, allowList: [], denyList: [] }); }, async getAllFlags() { return Array.from(this.flags.entries()).map(([name, config]) => ({ name, ...config })); } };
