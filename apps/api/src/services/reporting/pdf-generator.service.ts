export const pdfGeneratorService = { async generateInvoicePDF(invoice: any): Promise<Buffer> { console.log(`[PDF] Generating invoice PDF for ${invoice.id}`); return Buffer.from("PDF content"); }, async generateReportPDF(report: any): Promise<Buffer> { console.log(`[PDF] Generating report PDF for ${report.id}`); return Buffer.from("PDF content"); }, async generateCertificatePDF(data: { name: string; course: string; date: string }): Promise<Buffer> { return Buffer.from("PDF content"); } };
