export const powerManagementService = { async shutdown(deviceId: string, delay?: number) { console.log(`[Power] Shutting down device ${deviceId}${delay ? ` in ${delay}s` : ""}`); return { command: "shutdown", scheduled: true, deviceId }; }, async restart(deviceId: string, delay?: number) { console.log(`[Power] Restarting device ${deviceId}`); return { command: "restart", scheduled: true, deviceId }; }, async sleep(deviceId: string) { return { command: "sleep", executed: true, deviceId }; }, async hibernate(deviceId: string) { return { command: "hibernate", executed: true, deviceId }; }, async scheduleWake(deviceId: string, wakeAt: Date) { return { scheduled: true, wakeAt, deviceId }; }, async getSchedule(deviceId: string) { return { shutdownSchedule: null, wakeSchedule: null }; } };
