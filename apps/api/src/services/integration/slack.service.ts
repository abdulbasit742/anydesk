export const slackService = { async sendMessage(channel: string, text: string, blocks?: any[]) { const webhookUrl = process.env.SLACK_WEBHOOK_URL; if (!webhookUrl) return { sent: false, reason: "No webhook configured" }; await fetch(webhookUrl, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ channel, text, blocks }) }); return { sent: true }; }, async sendAlert(severity: string, message: string, details?: string) { const emoji = { critical: "🚨", high: "⚠️", medium: "📢", low: "ℹ️" }[severity] || "📢"; return this.sendMessage("#alerts", `${emoji} [${severity.toUpperCase()}] ${message}${details ? `\n${details}` : ""}`); } };
