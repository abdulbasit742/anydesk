export const pagerdutyService = { async triggerIncident(routingKey: string, summary: string, severity: string, source: string) { await fetch("https://events.pagerduty.com/v2/enqueue", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ routing_key: routingKey, event_action: "trigger", payload: { summary, severity, source, timestamp: new Date().toISOString() } }) }); return { triggered: true }; }, async resolveIncident(routingKey: string, dedupKey: string) { await fetch("https://events.pagerduty.com/v2/enqueue", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ routing_key: routingKey, event_action: "resolve", dedup_key: dedupKey }) }); return { resolved: true }; } };
