export const fileTransferService = { async initiateTransfer(sessionId: string, file: { name: string; size: number; type: string }) { return { transferId: `ft_${Date.now()}`, status: "pending", file }; }, async uploadChunk(transferId: string, chunk: Buffer, offset: number) { return { received: chunk.length, totalReceived: offset + chunk.length }; }, async completeTransfer(transferId: string) { return { transferId, status: "completed", completedAt: new Date() }; }, async cancelTransfer(transferId: string) { return { transferId, status: "cancelled" }; }, async getTransferProgress(transferId: string) { return { transferId, progress: 75, speed: 5 * 1024 * 1024, eta: 30 }; }, async listTransfers(sessionId: string) { return { transfers: [], total: 0 }; } };
