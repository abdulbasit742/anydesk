export const recordingService = { async startRecording(sessionId: string, options: { quality: string; fps: number }) { console.log(`[Recording] Starting for session ${sessionId}`); return { recordingId: `rec_${Date.now()}`, status: "recording", startedAt: new Date() }; }, async stopRecording(recordingId: string) { console.log(`[Recording] Stopping ${recordingId}`); return { recordingId, status: "processing", stoppedAt: new Date() }; }, async getRecording(recordingId: string) { return { id: recordingId, url: `https://storage.remotedesk.io/recordings/${recordingId}.webm`, duration: 3600, size: 150 * 1024 * 1024, createdAt: new Date() }; }, async listRecordings(userId: string, page: number = 1, limit: number = 20) { return { recordings: [], total: 0, page, limit }; }, async deleteRecording(recordingId: string) { return { deleted: true }; } };
