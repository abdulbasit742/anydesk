export const pushService = { async sendToUser(userId: string, title: string, body: string, data?: any) { console.log(`[Push] Sending to user ${userId}: ${title}`); return { sent: true, deviceCount: 2 }; }, async sendToDevice(deviceToken: string, title: string, body: string) { console.log(`[Push] Sending to device token`); return { sent: true }; }, async sendBulk(userIds: string[], title: string, body: string) { console.log(`[Push] Bulk sending to ${userIds.length} users`); return { sent: userIds.length, failed: 0 }; }, async registerToken(userId: string, token: string, platform: "ios" | "android" | "web") { console.log(`[Push] Registered ${platform} token for ${userId}`); return { registered: true }; } };
