export const smsService = { async send(phoneNumber: string, message: string) { console.log(`[SMS] Sending to ${phoneNumber}: ${message}`); return { sid: `sms_${Date.now()}`, status: "sent" }; }, async sendVerification(phoneNumber: string) { const code = Math.floor(100000 + Math.random() * 900000).toString(); console.log(`[SMS] Verification code to ${phoneNumber}: ${code}`); return { code, expiresAt: new Date(Date.now() + 600000) }; }, async sendAlert(phoneNumber: string, severity: string, message: string) { return this.send(phoneNumber, `[RemoteDesk ${severity.toUpperCase()}] ${message}`); } };
