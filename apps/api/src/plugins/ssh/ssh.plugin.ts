export const sshPlugin = { name: "ssh", version: "1.0.0", protocols: ["ssh"], defaultPort: 22, async connect(host: string, port: number, auth: { type: "password" | "key"; username: string; password?: string; privateKey?: string }) { return { sessionId: `ssh_${Date.now()}`, status: "connected", protocol: "ssh", encryption: "aes256-ctr" }; }, async disconnect(sessionId: string) { return { disconnected: true }; }, async executeCommand(sessionId: string, command: string) { return { stdout: "", stderr: "", exitCode: 0 }; }, getCapabilities() { return ["terminal", "file_transfer", "port_forwarding", "x11_forwarding"]; } };
