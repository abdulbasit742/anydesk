export const appConfig = { name: "RemoteDesk API", version: "3.0.0", port: parseInt(process.env.PORT || "3001"), env: process.env.NODE_ENV || "development", cors: { origins: ["http://localhost:5173", "http://localhost:3000", "https://app.remotedesk.io"], credentials: true }, rateLimit: { windowMs: 60000, max: 100 }, upload: { maxFileSize: 100 * 1024 * 1024, allowedTypes: ["image/*", "application/pdf", "application/zip", "text/*"] }, session: { maxDuration: 8 * 3600, idleTimeout: 1800, maxConcurrent: 5 } };
