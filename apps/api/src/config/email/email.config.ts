export const emailConfig = { provider: process.env.EMAIL_PROVIDER || "smtp", smtp: { host: process.env.SMTP_HOST || "smtp.gmail.com", port: parseInt(process.env.SMTP_PORT || "587"), secure: false, auth: { user: process.env.SMTP_USER || "", pass: process.env.SMTP_PASS || "" } }, from: { name: "RemoteDesk", email: "noreply@remotedesk.io" }, templates: { dir: "./templates/email" } };
