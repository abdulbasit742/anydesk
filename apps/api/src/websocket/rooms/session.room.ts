export const sessionRoom = { join(socket: any, sessionId: string) { socket.join(`session:${sessionId}`); console.log(`[WS] ${socket.id} joined session ${sessionId}`); }, leave(socket: any, sessionId: string) { socket.leave(`session:${sessionId}`); console.log(`[WS] ${socket.id} left session ${sessionId}`); }, broadcast(io: any, sessionId: string, event: string, data: any) { io.to(`session:${sessionId}`).emit(event, data); } };
