export const connectionHandler = { onConnect(socket: any) { console.log(`[WS] Client connected: ${socket.id}`); socket.emit("welcome", { message: "Connected to RemoteDesk", timestamp: Date.now() }); }, onDisconnect(socket: any, reason: string) { console.log(`[WS] Client disconnected: ${socket.id} (${reason})`); }, onError(socket: any, error: Error) { console.error(`[WS] Error on ${socket.id}:`, error.message); } };
