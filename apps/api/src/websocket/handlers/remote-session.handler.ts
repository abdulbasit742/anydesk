export const remoteSessionHandler = { onScreenFrame(socket: any, data: { frame: Buffer; width: number; height: number }) { socket.to(data.sessionId).emit("screen_frame", data); }, onMouseInput(socket: any, data: { x: number; y: number; button: number; action: string }) { socket.to(data.sessionId).emit("mouse_input", data); }, onKeyboardInput(socket: any, data: { key: string; action: "down" | "up"; modifiers: string[] }) { socket.to(data.sessionId).emit("keyboard_input", data); }, onClipboardSync(socket: any, data: { content: string; type: string }) { socket.to(data.sessionId).emit("clipboard_sync", data); } };
