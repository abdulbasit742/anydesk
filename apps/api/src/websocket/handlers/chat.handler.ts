export const chatHandler = { onMessage(socket: any, data: { sessionId: string; message: string; userId: string }) { const msg = { ...data, timestamp: Date.now(), id: `msg_${Date.now()}` }; socket.to(`session:${data.sessionId}`).emit("chat_message", msg); }, onTyping(socket: any, data: { sessionId: string; userId: string; isTyping: boolean }) { socket.to(`session:${data.sessionId}`).emit("typing", data); } };
