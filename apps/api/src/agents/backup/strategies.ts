export const backupStrategies = { full: { name: "Full Backup", description: "Complete system backup", frequency: "weekly", retentionDays: 90, estimatedSize: "100%" }, incremental: { name: "Incremental Backup", description: "Only changes since last backup", frequency: "daily", retentionDays: 30, estimatedSize: "5-15%" }, differential: { name: "Differential Backup", description: "Changes since last full backup", frequency: "daily", retentionDays: 30, estimatedSize: "10-40%" }, snapshot: { name: "Snapshot", description: "Point-in-time system state", frequency: "hourly", retentionDays: 7, estimatedSize: "1-5%" } };
export const getRecommendedStrategy = (deviceType: string, dataSize: number): string => { if (dataSize > 500 * 1024 * 1024 * 1024) return "incremental"; if (deviceType === "server") return "differential"; return "full"; };
