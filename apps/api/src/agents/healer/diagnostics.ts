export const diagnosticChecks = { async checkDiskSpace(threshold: number = 90): Promise<{ ok: boolean; usage: number; path: string }> { return { ok: true, usage: 45, path: "/" }; }, async checkMemory(threshold: number = 90): Promise<{ ok: boolean; usage: number; available: number }> { return { ok: true, usage: 60, available: 4096 }; }, async checkCPU(threshold: number = 90): Promise<{ ok: boolean; usage: number; loadAvg: number[] }> { return { ok: true, usage: 30, loadAvg: [1.2, 0.8, 0.5] }; }, async checkServices(services: string[]): Promise<Array<{ name: string; running: boolean; pid?: number }>> { return services.map(s => ({ name: s, running: true, pid: 1234 })); }, async checkNetwork(): Promise<{ ok: boolean; latency: number; packetLoss: number }> { return { ok: true, latency: 15, packetLoss: 0 }; } };
